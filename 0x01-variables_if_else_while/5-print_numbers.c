#include <stdio.h>
/* when declare a variable its normally a const to make it vary u  need format specializers*/
int main ()
{
	int i = 0;
	for (i = 0; i < 10 ; i++)
	{
		printf("%d",i);
	}
	printf("\n");
	return 0;
}
